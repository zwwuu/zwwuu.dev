const Footer = () => {
  return (
    <footer className={"mx-auto max-w-screen-xl px-4 py-8"}>
      <p className={"text-center"}>{`zwwuu © ${new Date().getFullYear()}`}</p>
    </footer>
  );
};

export default Footer;
